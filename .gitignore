# Environment — never commit real keys
.env.local
.env.*.local

# Project-specific exclusions
RAWDATA/
CLAUDE.md

# Vite / build
dist/
dist-ssr/

# TypeScript incremental build cache — machine-specific, regenerated every build
*.tsbuildinfo

# Node
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# pnpm
.pnpmfile.cjs

# Audit backlog — documents unpatched security issues on a live deployment.
# Un-ignore once TODO.md section 2 (Security) is resolved.
TODO.md

# Supabase CLI scratch files — leak project ref + pooler connection string
supabase/.temp/

# Claude Code — machine-local overrides only; the shared config is .claude/settings.json
.claude/settings.local.json